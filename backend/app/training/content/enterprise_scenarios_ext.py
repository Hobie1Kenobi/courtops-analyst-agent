"""Extended enterprise scenarios M4-M7, T5-T7, E4-E6 — completing the full 20."""

ENTERPRISE_SCENARIOS_EXT = {

    # ================================================================
    #  MAXIMO M4-M7
    # ================================================================

    "maximo_asset_reconciliation": {
        "name": "M4: Maximo Asset Transfer Reconciliation",
        "system": "maximo",
        "description": "23 fleet vehicles show different locations in Maximo vs. motor pool. SQL reconciliation + report needed.",
        "phases": ["investigation", "reconciliation"],
        "tasks": [{
            "title": "M4: Reconcile Fleet Asset Locations — Maximo vs Motor Pool",
            "domain": "sql_reporting",
            "business_context": "Fleet Manager reports 23 vehicles show different locations in Maximo versus the motor pool tracking spreadsheet. Monthly asset audit is due to the City Auditor in 3 days.",
            "description": "Write SQL to compare Maximo asset locations against the expected motor pool data, identify discrepancies, produce a reconciliation report.",
            "evidence_reviewed": "Motor pool spreadsheet lists 45 vehicles with current bay assignments. Maximo mx_asset table has location records for the same 45 vehicles. 23 have mismatched locations. Common pattern: vehicles were moved for maintenance but location was updated in the spreadsheet, not in Maximo.",
            "decision_made": "Bulk-update Maximo asset locations to match motor pool assignments, generate reconciliation report showing before/after, create a change request for real-time location sync.",
            "technical_detail": "-- Find mismatched locations\nSELECT a.assetnum, a.description,\n       a.location AS maximo_location,\n       mp.bay_location AS motorpool_location\nFROM mx_asset a\nJOIN motor_pool_current mp ON a.assetnum = mp.asset_tag\nWHERE a.location != mp.bay_location\nORDER BY a.assetnum;\n\n-- In training: compare against expected locations in seed data",
            "sql_or_config": "-- Bulk update locations\nUPDATE mx_asset\nSET location = mp.bay_location\nFROM motor_pool_current mp\nWHERE mx_asset.assetnum = mp.asset_tag\n  AND mx_asset.location != mp.bay_location;\n\n-- Generate reconciliation report\nSELECT a.assetnum, a.description, a.assettype,\n       'FLEET-YARD' AS old_location,\n       a.location AS new_location,\n       'Motor Pool Sync' AS reason\nFROM mx_asset a\nWHERE a.assettype = 'VEHICLE'\nORDER BY a.assetnum;",
            "why_correct": "Asset location accuracy is critical for fleet management — if Maximo shows a truck at Fleet Yard but it's actually at Parks HQ, work orders get routed to the wrong location. The reconciliation approach is: compare both sources, identify deltas, update the system of record (Maximo), and generate an auditable report showing every change.",
            "what_to_document": "1. Reconciliation report: 23 assets with before/after locations\n2. Bulk update script with rollback capability\n3. Change request: implement real-time motor pool → Maximo location sync\n4. Audit evidence package for City Auditor",
            "what_to_test": "1. Verify all 23 assets now show correct locations in Maximo\n2. Run a work order for a recently-moved vehicle — confirm correct location\n3. Compare Maximo asset count with motor pool roster — should match\n4. Verify audit report formatting meets City Auditor requirements",
            "mentor_explanation": "Asset reconciliation is a routine but critical task. The City Auditor verifies that the city's asset records match reality — discrepancies can indicate mismanagement or even theft. As an analyst, your job is to make the data match, document every change, and recommend process improvements to prevent future drift. The SQL skill here is writing comparison queries (JOIN + WHERE mismatch) and bulk updates with audit trails.",
            "interview_answer": "STAR: Situation — Fleet Manager reported 23 vehicles with location mismatches between Maximo and the motor pool system, with a City Auditor deadline in 3 days. Task — Reconcile all asset locations and produce an audit-ready report. Action — Wrote a comparison query joining Maximo and motor pool data, identified all 23 discrepancies (all caused by maintenance moves not reflected in Maximo). Performed a bulk update with before/after tracking, generated a reconciliation report, and recommended implementing real-time location sync. Result — All locations corrected, audit report delivered 2 days early. Location sync project approved for next quarter.",
            "skills_practiced": "sql_server,debugging,process_docs,change_mgmt",
            "phase": "investigation",
        }],
    },

    "maximo_slow_queries": {
        "name": "M5: Maximo Slow Work Order Query Performance",
        "system": "maximo",
        "description": "Users report 30-second load times on Work Order search. Database needs performance tuning.",
        "phases": ["investigation", "resolution"],
        "tasks": [{
            "title": "M5: Tune Maximo Work Order Query Performance",
            "domain": "sql_reporting",
            "business_context": "Multiple Maximo users report the Work Order List screen takes 30+ seconds to load. This affects all departments. The IT Director has escalated this as a P1 performance issue.",
            "description": "Identify the slow queries, check execution plans, update statistics, rebuild indexes, verify improvement.",
            "evidence_reviewed": "SQL Server Activity Monitor shows expensive queries on mx_workorder table. Most costly query: full table scan on 40,000 rows when filtering by status + location. Missing index on the (status, location) combination. Database statistics last updated 60 days ago.",
            "decision_made": "Create a composite index on (status, location), update all statistics with FULLSCAN, and rebuild fragmented indexes.",
            "technical_detail": "-- Find the slow query in Activity Monitor\nSELECT TOP 10\n    qs.total_elapsed_time / qs.execution_count AS avg_elapsed_ms,\n    qs.execution_count,\n    SUBSTRING(qt.text, 1, 200) AS query_text\nFROM sys.dm_exec_query_stats qs\nCROSS APPLY sys.dm_exec_sql_text(qs.sql_handle) qt\nORDER BY avg_elapsed_ms DESC;\n\n-- Check for missing indexes\nSELECT\n    d.statement AS table_name,\n    d.equality_columns,\n    d.inequality_columns,\n    s.avg_user_impact\nFROM sys.dm_db_missing_index_details d\nJOIN sys.dm_db_missing_index_groups g ON d.index_handle = g.index_handle\nJOIN sys.dm_db_missing_index_group_stats s ON g.index_group_handle = s.group_handle\nWHERE d.statement LIKE '%mx_workorder%'\nORDER BY s.avg_user_impact DESC;",
            "sql_or_config": "-- Step 1: Create the missing composite index\nCREATE NONCLUSTERED INDEX IX_mx_workorder_status_location\nON mx_workorder (status, location)\nINCLUDE (wonum, description, reportdate, assetnum, priority);\n\n-- Step 2: Update all statistics\nUPDATE STATISTICS mx_workorder WITH FULLSCAN;\nUPDATE STATISTICS mx_asset WITH FULLSCAN;\nUPDATE STATISTICS mx_pm WITH FULLSCAN;\n\n-- Step 3: Rebuild fragmented indexes\nALTER INDEX ALL ON mx_workorder REBUILD;\n\n-- Step 4: Verify improvement\n-- Before: 30,000ms (full table scan)\n-- After: 45ms (index seek)\nSET STATISTICS IO ON;\nSELECT wonum, description, status, location\nFROM mx_workorder\nWHERE status = 'INPRG' AND location = 'WATER-MAIN';\nSET STATISTICS IO OFF;",
            "why_correct": "When a query does a full table scan on a large table, every row is read from disk even if only 50 match the filter. A composite index on (status, location) lets SQL Server jump directly to matching rows — turning a 30-second scan into a 45ms seek. The INCLUDE columns cover the SELECT list so SQL Server doesn't need to go back to the main table (covering index). Statistics must be updated so the query optimizer knows about the new index.",
            "what_to_document": "1. Performance incident: INC-2026-0170 — Maximo WO search 30+ seconds\n2. Root cause: Missing composite index + stale statistics\n3. Fix: Created IX_mx_workorder_status_location, updated stats, rebuilt indexes\n4. Performance improvement: 30,000ms → 45ms (99.85% improvement)\n5. Maintenance: Weekly UPDATE STATISTICS job scheduled",
            "what_to_test": "1. Verify WO search loads in under 2 seconds for all departments\n2. Check execution plan shows Index Seek (not Scan)\n3. Run load test with 10 concurrent users — verify consistent performance\n4. Monitor SQL Server wait stats for 24 hours for any regression",
            "mentor_explanation": "Database performance tuning is one of the highest-value skills for an applications analyst. A single well-placed index can turn a 30-second query into a sub-second one. The diagnostic process: (1) Use Activity Monitor or DMVs to find the slow query. (2) Check the execution plan — look for Table Scans and Key Lookups. (3) Check sys.dm_db_missing_index_details for SQL Server's own index recommendations. (4) Create the index, update statistics, verify. Always use INCLUDE columns to create covering indexes when possible.",
            "interview_answer": "STAR: Situation — All Maximo users reported 30+ second load times on the Work Order search screen, escalated as a P1 by the IT Director. Task — Diagnose and resolve the performance issue. Action — Used SQL Server DMVs to identify a full table scan on 40,000 rows caused by a missing composite index. Created a covering index on (status, location) with INCLUDE columns, updated statistics with FULLSCAN, and rebuilt fragmented indexes. Result — Query time dropped from 30,000ms to 45ms — a 99.85% improvement. Established a weekly statistics maintenance schedule to prevent recurrence.",
            "skills_practiced": "sql_server,debugging,process_docs",
            "phase": "investigation",
        }],
    },

    "maximo_reorder_cron": {
        "name": "M6: Maximo Inventory Reorder Point Failure",
        "system": "maximo",
        "description": "ReorderCron not triggering purchase requisitions when stock falls below minimum. Warehouse running low.",
        "phases": ["investigation", "resolution"],
        "tasks": [{
            "title": "M6: Fix Maximo Inventory Reorder Cron Task",
            "domain": "infrastructure",
            "business_context": "Warehouse Supervisor reports that automatic purchase requisitions are not being generated when inventory items fall below their reorder points. Three critical parts for Water Utility pumps are nearly out of stock.",
            "description": "Investigate the REORDERCRON task, identify why reorder point triggers are failing, fix the configuration.",
            "evidence_reviewed": "REORDERCRON task is ACTIVE but the instance configuration has an incorrect storeroom filter. It's set to SITEID = 'MAIN' but the actual warehouse siteid is 'CCTX'. This means the cron task queries an empty result set every time it runs.",
            "decision_made": "Update the REORDERCRON cron task instance parameter from SITEID='MAIN' to SITEID='CCTX'. Manually trigger reorder for the 3 critical parts.",
            "technical_detail": "-- Check cron task configuration\nSELECT crontaskname, active, schedule, instancename\nFROM mx_crontaskdef\nWHERE crontaskname = 'REORDERCRON';\n\n-- The instance parameter 'SITEID' is set to 'MAIN'\n-- but our site ID is 'CCTX'\n-- This means: SELECT * FROM inventory WHERE siteid = 'MAIN' → 0 rows\n-- It should be: SELECT * FROM inventory WHERE siteid = 'CCTX'",
            "sql_or_config": "-- Step 1: Fix cron task instance parameter\n-- Cron Task Setup > REORDERCRON > Instance REORDER01\n-- Change parameter SITEID from 'MAIN' to 'CCTX'\n\n-- Step 2: Verify the fix\n-- Run REORDERCRON manually > Check log for 'Processing X items below reorder point'\n\n-- Step 3: Generate emergency PO for critical parts\n-- Inventory > Reorder > Select items below minimum\n-- Create Purchase Requisition for:\n--   Pump Seal Kit (PN-4401) - current: 2, min: 10\n--   Impeller Assembly (PN-4402) - current: 1, min: 5\n--   Bearing Set (PN-4403) - current: 0, min: 8",
            "why_correct": "Cron task instance parameters are a common configuration trap in Maximo. The task itself can be ACTIVE with a valid schedule, but if an instance parameter is wrong, the task runs successfully (no error) but processes zero records. This is a 'silent failure' — the hardest kind to detect because nothing appears broken in the logs. The fix is straightforward (correct the parameter), but the diagnostic skill is knowing to check instance parameters, not just the task status.",
            "what_to_document": "1. Incident: INC-2026-0173 — Inventory reorder automation failing silently\n2. Root cause: REORDERCRON instance parameter SITEID = 'MAIN' (incorrect), should be 'CCTX'\n3. Fix: Updated parameter, manually triggered reorder for 3 critical parts\n4. Emergency PO raised for pump maintenance parts\n5. Validation: Added SITEID check to quarterly cron task audit checklist",
            "what_to_test": "1. Run REORDERCRON manually and verify purchase requisitions are generated\n2. Check the 3 critical parts show pending POs\n3. Verify the cron task log shows 'Processing N items'\n4. Monitor next automated run (overnight)\n5. Verify no other cron tasks have incorrect SITEID parameters",
            "mentor_explanation": "Silent failures are the most dangerous kind. The system looks healthy — the cron task is ACTIVE, it runs on schedule, there are no errors in the log. But it processes zero records because a parameter is wrong. This is why experienced analysts don't just check 'is it running?' — they check 'is it doing what it should be doing?' Always verify output, not just status. In Maximo, cron task instance parameters are the #1 source of silent configuration errors.",
            "interview_answer": "STAR: Situation — Warehouse supervisor reported inventory reorder automation had stopped working, with 3 critical pump parts nearly out of stock. No errors appeared in any logs. Task — Diagnose the silent failure and restore automated reordering before parts ran out. Action — Investigated the REORDERCRON cron task — it was ACTIVE and running on schedule, but the instance parameter SITEID was set to 'MAIN' instead of 'CCTX', causing it to query an empty dataset. Corrected the parameter, triggered manual reorder, and raised emergency POs for the critical parts. Result — Reorder automation restored. Emergency parts ordered same day. Added SITEID validation to the quarterly cron task audit to catch similar silent failures across all cron tasks.",
            "skills_practiced": "sql_server,debugging,process_docs,change_mgmt",
            "phase": "investigation",
        }],
    },

    "maximo_meter_integration": {
        "name": "M7: Maximo New Meter Reading Integration",
        "system": "maximo",
        "description": "Water Utility wants to import IoT sensor meter readings into Maximo. Requirements gathering + integration design.",
        "phases": ["requirements", "design"],
        "tasks": [{
            "title": "M7: Design IoT Meter Reading Integration for Water Utility",
            "domain": "requirements_docs",
            "business_context": "Water Utility has deployed 500 IoT sensors on water meters throughout the city. They want to automatically import meter readings into Maximo to trigger condition-based maintenance. Currently, technicians manually enter readings after physical inspections.",
            "description": "Gather requirements, design the integration approach using Maximo Integration Framework, create the functional specification.",
            "evidence_reviewed": "IoT platform exports CSV files every 4 hours with columns: sensor_id, meter_id, reading_value, reading_unit, timestamp, battery_pct. Maximo already has meter groups defined for water assets. The integration should use Maximo's REST API to create meter readings on assets.",
            "decision_made": "Design a scheduled integration using Maximo's Integration Framework: IoT platform → CSV file → ETL script → Maximo REST API → Meter Readings on Assets. Use the MXMETER Object Structure.",
            "technical_detail": "Integration Architecture:\n\n1. IoT Platform → SFTP Drop (CSV every 4 hours)\n2. PowerShell ETL script:\n   - Read CSV\n   - Validate sensor_id → Maximo assetnum mapping\n   - Transform to Maximo MXMETER JSON format\n   - POST to Maximo REST API\n3. Maximo REST API endpoint:\n   POST /maximo/api/os/mxmeter\n   {\n     \"assetnum\": \"WP-2001\",\n     \"metername\": \"FLOW-RATE\",\n     \"meterreading\": 1523.4,\n     \"readingdate\": \"2026-02-25T14:00:00\"\n   }",
            "sql_or_config": "# PowerShell ETL Script (simplified)\n$csvData = Import-Csv 'C:\\IoT\\meter_readings_20260225.csv'\n$apiUrl = 'https://maximo.cctx.gov/maximo/api/os/mxmeter'\n$headers = @{\n    'Authorization' = 'Bearer {API_KEY}'\n    'Content-Type' = 'application/json'\n}\n\nforeach ($row in $csvData) {\n    $body = @{\n        assetnum = $sensorMap[$row.sensor_id]\n        metername = 'FLOW-RATE'\n        meterreading = [float]$row.reading_value\n        readingdate = $row.timestamp\n    } | ConvertTo-Json\n\n    try {\n        Invoke-RestMethod -Uri $apiUrl -Method POST `\n            -Headers $headers -Body $body\n    } catch {\n        Write-Log \"ERROR: $($row.sensor_id) - $($_.Exception.Message)\"\n    }\n}",
            "why_correct": "Using Maximo's REST API with the MXMETER Object Structure is the standard pattern for external meter reading integration. It leverages Maximo's built-in validation (meter exists, reading is within range, reading date is valid) and automatically triggers condition-based PM generation if thresholds are exceeded. The PowerShell ETL approach is simple, scriptable, and easy to maintain.",
            "what_to_document": "1. Functional Specification: IoT Meter Reading Integration\n   - Business requirements\n   - Data mapping (IoT fields → Maximo fields)\n   - Integration architecture diagram\n   - Error handling approach\n   - Testing plan\n2. Technical Design Document\n3. Sensor-to-Asset mapping table\n4. PowerShell ETL script with comments\n5. Test plan with sample data",
            "what_to_test": "1. Unit test: Parse sample CSV, verify JSON output format\n2. Integration test: POST 10 readings to Maximo staging\n3. Validation test: Submit an out-of-range reading, verify Maximo rejects it\n4. Volume test: Process 500 readings in one batch\n5. End-to-end: Verify PM auto-generated when reading exceeds threshold",
            "mentor_explanation": "Requirements gathering for integrations is a core analyst skill. You need to understand both the source system (IoT platform capabilities, data format, delivery schedule) and the target system (Maximo's Integration Framework, Object Structures, REST API). The functional spec must cover: what data flows, how it's transformed, what happens when errors occur, and how you'll test it. This scenario teaches the full lifecycle: from a business request ('we want meter readings in Maximo') to a working technical design.",
            "interview_answer": "STAR: Situation — Water Utility deployed 500 IoT sensors and wanted automated meter readings in Maximo to replace manual inspections and enable condition-based maintenance. Task — Gather requirements and design the integration. Action — Met with the IoT team to understand their data format and delivery schedule. Designed an integration using PowerShell ETL to transform CSV exports into Maximo REST API calls against the MXMETER Object Structure. Created a functional specification covering data mapping, error handling, and testing. Built the ETL script and tested with sample data. Result — Integration deployed successfully, processing 2,000 readings per day. Condition-based PMs now auto-generate when readings exceed thresholds, replacing 40 hours/month of manual meter inspections.",
            "skills_practiced": "requirements,web_services,powershell,process_docs,change_mgmt",
            "phase": "requirements",
        }],
    },

    # ================================================================
    #  TYLER INCODE T5-T7
    # ================================================================

    "incode_stats_mismatch": {
        "name": "T5: Incode Court Statistics Report Mismatch",
        "system": "incode",
        "description": "Monthly report shows 15% fewer dispositions than judge's tracking. JOIN bug excluding dismissed cases.",
        "phases": ["investigation", "resolution"],
        "tasks": [{
            "title": "T5: Fix Court Statistics Report — Missing Dismissed Cases",
            "domain": "sql_reporting",
            "business_context": "Judge Hernandez reports the monthly court statistics show 15% fewer dispositions than her personal tracking. This affects the court's efficiency metrics reported to the Texas Office of Court Administration.",
            "description": "Compare report query against raw data, identify the filtering bug, fix the stored procedure.",
            "evidence_reviewed": "Report stored procedure sp_MonthlyCourtStats filters dispositions using: WHERE disposition_code IS NOT NULL AND status = 'DISPOSED'. But dismissed cases have status = 'DISMISSED', not 'DISPOSED'. The judge counts both as dispositions (any case that reached a final outcome).",
            "decision_made": "The WHERE clause is too restrictive. It should include both DISPOSED and DISMISSED statuses, matching the legal definition of 'disposition' used by the Texas Office of Court Administration.",
            "technical_detail": "-- Current query (wrong):\nSELECT COUNT(*) AS dispositions\nFROM ic_case\nWHERE disposition_date IS NOT NULL\n  AND status = 'DISPOSED'  -- ← Misses DISMISSED cases!\n  AND court_date BETWEEN @StartDate AND @EndDate;\n\n-- Correct query:\nSELECT COUNT(*) AS dispositions\nFROM ic_case\nWHERE disposition_date IS NOT NULL\n  AND status IN ('DISPOSED', 'DISMISSED', 'PAID')  -- All final outcomes\n  AND court_date BETWEEN @StartDate AND @EndDate;",
            "sql_or_config": "-- Fix the stored procedure\nALTER PROCEDURE sp_MonthlyCourtStats\n    @StartDate DATE,\n    @EndDate DATE\nAS\nBEGIN\n    SET NOCOUNT ON;\n    SELECT\n        COUNT(*) AS total_cases,\n        SUM(CASE WHEN status IN ('DISPOSED','DISMISSED','PAID')\n            THEN 1 ELSE 0 END) AS dispositions,\n        SUM(CASE WHEN status = 'OPEN' THEN 1 ELSE 0 END) AS pending,\n        SUM(CASE WHEN status IN ('FTA','WARRANT')\n            THEN 1 ELSE 0 END) AS fta_warrant,\n        ROUND(\n            SUM(CASE WHEN status IN ('DISPOSED','DISMISSED','PAID')\n                THEN 1.0 ELSE 0 END) / NULLIF(COUNT(*), 0) * 100, 1\n        ) AS disposition_rate_pct\n    FROM ic_case\n    WHERE court_date BETWEEN @StartDate AND @EndDate;\nEND;",
            "why_correct": "The legal definition of 'disposition' includes any final case outcome — not just 'disposed'. Dismissed cases, guilty pleas (PAID), and deferred dispositions are all final outcomes. The original query only counted one of several terminal statuses, underreporting by 15%. Always validate report definitions against the business/legal requirements, not just the database column names.",
            "what_to_document": "1. Defect report: Court statistics underreporting dispositions by 15%\n2. Root cause: WHERE clause filtered to status = 'DISPOSED' only\n3. Fix: Changed to IN ('DISPOSED','DISMISSED','PAID')\n4. Validated against judge's manual tracking — totals now match\n5. Updated report functional spec with correct disposition definition",
            "what_to_test": "1. Run report for current month — compare against judge's count\n2. Run for previous 6 months — verify all match historical actuals\n3. Run with empty date range — verify NULLIF prevents division by zero\n4. Verify Crystal Report layout displays new categories correctly",
            "mentor_explanation": "This is a perfect example of why analysts must understand the BUSINESS meaning of data, not just the technical column names. 'Disposition' has a specific legal definition in court operations that includes dismissals and pleas — not just the one status labeled 'DISPOSED' in the database. When a report doesn't match a subject matter expert's expectations, the first question is: does my query's definition match the business definition? The SQL fix is simple, but knowing WHAT to fix requires domain knowledge.",
            "interview_answer": "STAR: Situation — Judge Hernandez reported that monthly court statistics showed 15% fewer dispositions than her personal tracking, affecting efficiency metrics reported to the state. Task — Identify why the report was underreporting and fix it. Action — Compared the stored procedure's WHERE clause against the legal definition of 'disposition.' Found it only counted status = 'DISPOSED', missing 'DISMISSED' and 'PAID' cases — all of which are final outcomes under Texas court administration rules. Modified the procedure to include all terminal statuses. Result — Report matched the judge's count exactly. Updated the functional specification with the correct disposition definition and ran a 6-month historical validation to confirm accuracy.",
            "skills_practiced": "sql_server,stored_procedures,crystal_reports,debugging,process_docs",
            "phase": "investigation",
        }],
    },

    "incode_upgrade_testing": {
        "name": "T6: Tyler Incode Version Upgrade Testing",
        "system": "incode",
        "description": "Incode 10.x to 11.x upgrade in staging. Create test plan, run regression tests, validate data migration.",
        "phases": ["planning", "testing", "validation"],
        "tasks": [{
            "title": "T6: Plan and Execute Incode v11 Upgrade Testing",
            "domain": "qa_debugging",
            "business_context": "Tyler Technologies released Incode v11 with new online payment features and performance improvements. The Court Administrator approved the upgrade. You must create a test plan, execute it in staging, and produce a go/no-go recommendation.",
            "description": "Create comprehensive test plan, execute regression tests, validate data migration, document results with pass/fail evidence.",
            "evidence_reviewed": "Tyler release notes: 47 changes including 12 bug fixes, 8 enhancements, 27 internal improvements. Key changes: new payment gateway API, modified docket report, updated defendant search. Staging environment has a copy of production data from last weekend.",
            "decision_made": "Create a 5-category test plan covering: data migration validation, critical business process regression, new feature verification, performance comparison, and integration testing. Execute in staging with documented evidence for each test.",
            "technical_detail": "Test Plan Categories:\n\n1. DATA MIGRATION (8 tests)\n   - Case count: staging = production ± 0\n   - Payment totals: staging = production ± $0.01\n   - Defendant records: count match\n   - Warrant status preservation\n   - Docket schedule preservation\n   - Citation import history\n   - Audit log integrity\n   - GL account mappings\n\n2. REGRESSION (12 tests)\n   - Create new citation → case → hearing\n   - Process payment (cash, check, credit)\n   - Issue warrant, serve warrant, recall warrant\n   - Generate docket for each courtroom\n   - Run monthly statistics report\n   - Export data to CSV\n   - Search by defendant name\n   - Search by citation number\n   - Modify case status through full lifecycle\n   - Generate FTA letters\n   - Process batch citation import\n   - Verify RBAC permissions for each role",
            "sql_or_config": "-- Data migration validation queries\n-- Run these against BOTH production and staging, compare results\n\n-- Test 1: Case count\nSELECT COUNT(*) AS total_cases FROM ic_case;\n\n-- Test 2: Payment totals\nSELECT SUM(amount) AS total_payments,\n       COUNT(*) AS payment_count\nFROM ic_payment;\n\n-- Test 3: Status distribution\nSELECT status, COUNT(*) AS cnt\nFROM ic_case\nGROUP BY status\nORDER BY status;\n\n-- Test 4: Outstanding balance reconciliation\nSELECT SUM(balance_due) AS total_outstanding\nFROM ic_case\nWHERE balance_due > 0;",
            "why_correct": "Enterprise application upgrades require methodical testing because they touch every aspect of the system — data, business logic, integrations, and UI. The test plan must cover: (1) data integrity (did the migration preserve everything?), (2) regression (do existing features still work?), (3) new features (do they work as documented?), (4) performance (is it faster or slower?), and (5) integrations (do external connections still work?). Each test needs documented evidence — not just 'it works' but screenshots, query results, and timestamps.",
            "what_to_document": "1. Test Plan document with 35+ test cases\n2. Test Execution Report with pass/fail results and evidence\n3. Data Migration Validation Report (query results from both environments)\n4. Performance Comparison (response time before vs. after)\n5. Go/No-Go Recommendation memo to Court Administrator\n6. Rollback plan if issues are found in production",
            "what_to_test": "This IS the testing task. Execute all 35+ test cases in the plan with documented evidence for each.",
            "mentor_explanation": "Upgrade testing is one of the most important skills for an enterprise applications analyst. Vendors release updates regularly, and each one carries risk. Your test plan is your safety net. The key principle: test everything the system does that matters to the business. Prioritize by impact — payment processing and case management get tested first because failures there have the highest business impact. Always have a rollback plan, and always get the sign-off in writing before going to production.",
            "interview_answer": "STAR: Situation — Tyler Technologies released Incode v11, and the Court Administrator approved the upgrade. Task — Create and execute a comprehensive test plan and provide a go/no-go recommendation. Action — Developed a 35-test-case plan across 5 categories: data migration, regression, new features, performance, and integrations. Executed all tests in staging with documented evidence. Found 2 minor issues (docket formatting and search performance), worked with Tyler to resolve before go-live. Result — Upgrade deployed successfully with zero production issues. The test plan became the template for all future Tyler upgrades, reducing testing time by 40% for subsequent releases.",
            "skills_practiced": "debugging,process_docs,change_mgmt,requirements",
            "phase": "planning",
        }],
    },

    "incode_online_payment": {
        "name": "T7: Incode Online Payment Portal Configuration",
        "system": "incode",
        "description": "Configure new online payment portal for defendants to pay citations. End-to-end integration test.",
        "phases": ["configuration", "testing"],
        "tasks": [{
            "title": "T7: Configure and Test Online Citation Payment Portal",
            "domain": "app_integration",
            "business_context": "City Council approved an online citation payment portal so defendants can pay fines without visiting the court in person. The payment gateway vendor (PayGov) is ready. You need to configure the Incode side, test end-to-end, and document the process.",
            "description": "Configure Incode payment gateway settings, set up the API connection, test with sample transactions, verify payment posting and reconciliation.",
            "evidence_reviewed": "PayGov provided: API endpoint URL, merchant ID, API key, test card numbers. Incode has a Payment Gateway Configuration screen. Need to map PayGov transaction fields to Incode payment fields. Transaction types: fine payment, court cost payment, partial payment.",
            "decision_made": "Configure Incode's payment gateway module with PayGov credentials, map transaction fields, test with 5 sample payments covering all transaction types, verify posting and reconciliation.",
            "technical_detail": "Payment Gateway Configuration:\n\nIncode Admin > System Config > Payment Gateway\n- Provider: PayGov\n- Endpoint: https://api.paygov.us/v2/transactions\n- Merchant ID: CC-MUNI-COURT-001\n- API Key: [configured in secure settings]\n- Transaction Types: FINE, COURT_COST, PARTIAL\n\nField Mapping:\n  PayGov.transaction_id → ic_payment.gateway_txn_id\n  PayGov.amount → ic_payment.amount\n  PayGov.card_last4 → ic_payment.reference_number\n  PayGov.timestamp → ic_payment.payment_date\n  PayGov.citation_number → ic_payment.case_number (via lookup)",
            "sql_or_config": "-- Test Case 1: Full fine payment\n-- Citation: CIT-2026-10001, Fine: $195.00, Court Costs: $55.00\n-- Pay full amount: $250.00\n-- Expected: ic_payment.amount = 250.00, case.balance_due = 0.00\n\n-- Test Case 2: Partial payment\n-- Citation: CIT-2026-10002, Total Due: $350.00\n-- Pay $100.00\n-- Expected: ic_payment.amount = 100.00, case.balance_due = 250.00\n\n-- Test Case 3: Declined card\n-- Use test card 4000000000000002 (always decline)\n-- Expected: No payment record created, error returned to user\n\n-- Verification query:\nSELECT p.payment_id, p.case_number, p.amount,\n       p.gateway_txn_id, p.posted,\n       c.total_due, c.total_paid, c.balance_due\nFROM ic_payment p\nJOIN ic_case c ON p.case_number = c.case_number\nWHERE p.payment_source = 'ONLINE'\n  AND p.payment_date >= CURRENT_DATE;",
            "why_correct": "Online payment integrations require careful field mapping and thorough testing because they involve real money. The test cases cover the critical paths: full payment (happy path), partial payment (business rule validation), and declined card (error handling). Each test verifies both the payment record creation AND the case balance update. The reconciliation check ensures the gateway and Incode agree on the transaction.",
            "what_to_document": "1. Payment Gateway Configuration Guide\n2. Field Mapping Document (PayGov → Incode)\n3. Test Plan with 5 test cases and expected results\n4. Test Execution Report with evidence (screenshots + SQL results)\n5. User Guide: 'How to Pay Your Citation Online'\n6. Reconciliation Procedure for daily gateway settlement",
            "what_to_test": "1. Full payment — verify balance goes to $0\n2. Partial payment — verify correct remaining balance\n3. Declined card — verify no payment recorded\n4. Duplicate payment attempt — verify rejection\n5. Reconciliation — verify gateway total matches Incode total",
            "mentor_explanation": "Payment gateway integrations are high-stakes projects — they touch real money and public trust. The configuration seems simple (enter credentials, map fields), but the testing must be exhaustive. Always test the error cases (declined cards, duplicates, timeouts) more than the happy path. And always verify that the financial reconciliation works — the gateway's daily settlement must match Incode's payment records exactly. Documentation is critical because the court cashier needs step-by-step instructions for daily reconciliation.",
            "interview_answer": "STAR: Situation — City Council approved an online citation payment portal, and I was responsible for configuring and testing the Incode integration with the PayGov gateway. Task — Configure the gateway, test all transaction types, ensure financial accuracy, and document for court staff. Action — Configured the payment gateway module, mapped 6 transaction fields, and created a 5-case test plan covering full payment, partial payment, declined cards, duplicates, and reconciliation. Executed all tests in staging with documented evidence. Created a daily reconciliation procedure and user guide. Result — Portal launched on schedule with zero payment discrepancies in the first month. Reduced in-person court visits by 35%, improving public convenience and court efficiency.",
            "skills_practiced": "web_services,sql_server,debugging,process_docs,requirements",
            "phase": "configuration",
        }],
    },

    # ================================================================
    #  E-BUILDER E4-E6
    # ================================================================

    "ebuilder_rfi_stuck": {
        "name": "E4: e-Builder RFI Workflow Stuck",
        "system": "ebuilder",
        "description": "RFI process stuck at 'Pending Review' for 12 days. Workflow engine configuration issue.",
        "phases": ["investigation", "resolution"],
        "tasks": [{
            "title": "E4: Unstick e-Builder RFI Workflow — Oso Creek Project",
            "domain": "app_integration",
            "business_context": "Contractor on the Oso Creek Water Reclamation project submitted an RFI about foundation soil conditions 12 days ago. It's stuck at 'Pending Review' and the contractor is threatening a delay claim. The RFI SLA is 14 days.",
            "description": "Investigate why the RFI workflow is stuck, identify the routing failure, reassign and expedite the response.",
            "evidence_reviewed": "RFI-001-001 submitted by contractor, assigned to 'City Engineer' role. The City Engineer who was assigned (J. Salazar) went on leave 10 days ago. The workflow doesn't have a backup assignee or escalation rule. The RFI has cost_impact = TRUE and schedule_impact = TRUE.",
            "decision_made": "The workflow stalled because the assigned reviewer is on leave with no backup routing. Reassign to the backup engineer, escalate as urgent given the SLA deadline and cost/schedule impact, and add an escalation rule to the workflow.",
            "technical_detail": "-- Check stuck RFIs\nSELECT rfi_id, project_id, rfi_number, subject,\n       status, assigned_to, days_open,\n       cost_impact, schedule_impact\nFROM eb_rfi\nWHERE status IN ('OPEN', 'OVERDUE')\n  AND days_open > 7\nORDER BY days_open DESC;",
            "sql_or_config": "-- Step 1: Reassign the stuck RFI\nUPDATE eb_rfi\nSET assigned_to = 'Deputy City Engineer',\n    status = 'OPEN'\nWHERE rfi_number = 'RFI-001-001'\n  AND status = 'OPEN';\n\n-- Step 2: Add escalation rule to e-Builder workflow\n-- Admin > Workflows > RFI Process > Edit\n-- Add rule: IF days_open > 7 AND assignee inactive\n-- THEN reassign to role backup AND email manager\n\n-- Step 3: Check all other stuck RFIs\nSELECT rfi_number, subject, assigned_to, days_open\nFROM eb_rfi\nWHERE status IN ('OPEN', 'OVERDUE')\n  AND days_open > 7;",
            "why_correct": "Workflow automation is only as good as its exception handling. When a human step stalls (leave, resignation, overload), the workflow stops. The immediate fix is manual reassignment. The permanent fix is adding escalation rules that auto-reassign after a threshold (7 days). For RFIs with cost and schedule impact, the escalation should be faster (3 days) because contractor delay claims are expensive.",
            "what_to_document": "1. Incident: RFI-001-001 stuck for 12 days, contractor threatening delay claim\n2. Root cause: Assigned reviewer on leave, no backup routing in workflow\n3. Fix: Reassigned to Deputy City Engineer, added escalation rule\n4. Process improvement: All workflows now have backup assignee + escalation rules\n5. Response to contractor documenting the resolution",
            "what_to_test": "1. Verify RFI-001-001 is now assigned to Deputy City Engineer\n2. Submit a test RFI and verify routing works\n3. Simulate reviewer absence — verify escalation fires after 7 days\n4. Verify email notifications sent on escalation\n5. Check all 8 CIP projects for other stuck workflows",
            "mentor_explanation": "Workflow management is a critical skill for CIP project support. Construction projects depend on timely responses to RFIs, submittals, and change orders. When a workflow stalls, the financial impact can be enormous — contractor delay claims on a $45M project can cost hundreds of thousands of dollars. The technical fix (reassign + add escalation rule) is simple. The process maturity skill is recognizing that EVERY workflow needs exception handling — what happens when the assigned person is unavailable?",
            "interview_answer": "STAR: Situation — A construction RFI on a $45M water reclamation project was stuck for 12 days, with the contractor threatening a delay claim. Task — Unstick the RFI, prevent future occurrences, and address the contractor's concern. Action — Found the assigned reviewer was on leave with no backup routing configured. Immediately reassigned to the Deputy City Engineer. Added escalation rules to ALL CIP workflows: auto-reassign after 7 days (3 days for cost/schedule-impacting items). Documented the resolution and communicated to the contractor. Result — RFI responded to within 24 hours of reassignment. Escalation rules prevented 4 similar stalls in the following quarter. No delay claim was filed.",
            "skills_practiced": "process_docs,debugging,change_mgmt,requirements",
            "phase": "investigation",
        }],
    },

    "ebuilder_cip_report": {
        "name": "E5: e-Builder CIP Monthly Executive Report",
        "system": "ebuilder",
        "description": "Generate executive summary of all active CIP projects: budget vs. actual, schedule variance, risk flags.",
        "phases": ["data_collection", "report_generation"],
        "tasks": [{
            "title": "E5: Generate Monthly CIP Executive Summary Report",
            "domain": "sql_reporting",
            "business_context": "The City Manager requires a monthly CIP status report for the City Council meeting. The report must summarize all active projects: budget vs. actual, schedule status, risk flags, and key milestones. It should be one page of high-level metrics plus a project-by-project summary.",
            "description": "Query project data, calculate KPIs, generate the executive summary with visualizations.",
            "evidence_reviewed": "8 active CIP projects in e-Builder. Data needed: project name, status, budget, actual cost, % complete, schedule variance, open RFIs, pending change orders. Report template follows the City of Corpus Christi CIP reporting format.",
            "decision_made": "Generate a summary dashboard using SQL queries against e-Builder data, formatted as an executive report with RAG (Red/Amber/Green) status indicators for each project.",
            "technical_detail": "-- Executive Summary KPIs\nSELECT\n    COUNT(*) AS total_projects,\n    SUM(budget_total) AS total_budget,\n    SUM(actual_cost) AS total_spent,\n    SUM(budget_remaining) AS total_remaining,\n    ROUND(SUM(actual_cost) / NULLIF(SUM(budget_total), 0) * 100, 1) AS overall_pct_spent,\n    SUM(CASE WHEN schedule_variance_days > 14 THEN 1 ELSE 0 END) AS behind_schedule,\n    SUM(CASE WHEN actual_cost > budget_total THEN 1 ELSE 0 END) AS over_budget\nFROM eb_project\nWHERE status NOT IN ('COMPLETE', 'ON HOLD');",
            "sql_or_config": "-- Project-by-project summary with RAG status\nSELECT\n    project_id,\n    project_name,\n    status,\n    budget_total,\n    actual_cost,\n    budget_remaining,\n    percent_complete,\n    schedule_variance_days,\n    CASE\n        WHEN actual_cost > budget_total OR schedule_variance_days > 30 THEN 'RED'\n        WHEN actual_cost > budget_total * 0.9 OR schedule_variance_days > 14 THEN 'AMBER'\n        ELSE 'GREEN'\n    END AS rag_status,\n    CASE\n        WHEN actual_cost > budget_total THEN 'Over budget by $' || CAST(actual_cost - budget_total AS VARCHAR)\n        WHEN schedule_variance_days > 14 THEN 'Behind schedule by ' || schedule_variance_days || ' days'\n        ELSE 'On track'\n    END AS status_note\nFROM eb_project\nWHERE status NOT IN ('COMPLETE', 'ON HOLD')\nORDER BY budget_total DESC;\n\n-- Open risks summary\nSELECT p.project_id, p.project_name,\n    (SELECT COUNT(*) FROM eb_rfi r WHERE r.project_id = p.project_id AND r.status IN ('OPEN','OVERDUE')) AS open_rfis,\n    (SELECT COUNT(*) FROM eb_change_order co WHERE co.project_id = p.project_id AND co.status = 'PENDING') AS pending_cos,\n    (SELECT SUM(co.amount) FROM eb_change_order co WHERE co.project_id = p.project_id AND co.status = 'PENDING') AS pending_co_amount\nFROM eb_project p\nORDER BY p.budget_total DESC;",
            "why_correct": "Executive CIP reports must answer three questions in 30 seconds: (1) Are we on budget? (2) Are we on schedule? (3) What needs my attention? The RAG status (Red/Amber/Green) gives instant visual priority. The SQL uses CASE expressions to apply business rules: RED for over-budget or >30 days behind, AMBER for approaching thresholds, GREEN for on-track. Subqueries pull in related risk indicators (open RFIs, pending change orders) so the City Manager sees the full picture.",
            "what_to_document": "1. CIP Monthly Executive Summary (1-page dashboard)\n2. Project-by-project detail report with RAG status\n3. Risk register: open RFIs, pending change orders, schedule issues\n4. Recommended actions for RED and AMBER projects\n5. Budget forecast for remaining fiscal year",
            "what_to_test": "1. Verify all active projects appear in the report\n2. Verify budget/actual totals match e-Builder project records\n3. Verify RAG logic: manually check 2 RED, 2 AMBER, 2 GREEN projects\n4. Verify formatting meets City Council presentation standards",
            "mentor_explanation": "Executive reporting is where technical SQL skills meet business communication. The City Manager doesn't want to see raw data — they want a dashboard that answers: what's on track, what needs attention, and what do you recommend? The RAG status pattern is universal in project management. As an analyst, your value is translating 8 projects × 20 data points into a one-page story that a non-technical executive can act on in a 5-minute council briefing.",
            "interview_answer": "STAR: Situation — City Manager needed a monthly CIP status report for City Council covering 8 active projects totaling $90M. Task — Create an executive summary dashboard with budget, schedule, and risk status for each project. Action — Wrote SQL queries with CASE-based RAG status logic (Red/Amber/Green) based on budget and schedule thresholds. Incorporated open RFIs and pending change orders as risk indicators. Formatted as a one-page dashboard with project-by-project detail. Result — Report adopted as the standard CIP reporting format for City Council meetings. Identified 2 projects needing attention (schedule slippage), which received corrective action before becoming critical.",
            "skills_practiced": "sql_server,power_bi,process_docs,requirements",
            "phase": "data_collection",
        }],
    },

    "ebuilder_dept_onboard": {
        "name": "E6: e-Builder New Department Onboarding",
        "system": "ebuilder",
        "description": "Engineering Dept wants e-Builder access for a new bond project. Create accounts, roles, project template.",
        "phases": ["configuration", "documentation"],
        "tasks": [{
            "title": "E6: Onboard Engineering Department to e-Builder",
            "domain": "requirements_docs",
            "business_context": "The Engineering Department received $6.2M in federal grant funding for the Padre Island Seawall Repair Phase II. They need e-Builder access to manage the project. Three staff members need accounts with appropriate roles.",
            "description": "Create user accounts, assign roles, configure the project template, set up workflows, and document the onboarding process.",
            "evidence_reviewed": "Three users requested: R. Thompson (Project Manager — full access), K. Patel (Inspector — view + document upload), S. Chen (Admin Assistant — view only + report generation). Project follows the standard CIP template with additional federal grant compliance requirements.",
            "decision_made": "Create 3 accounts with role-based access, clone the CIP project template with federal grant addenda, configure approval workflows, and provide training documentation.",
            "technical_detail": "User Role Matrix:\n\nUser          | Role               | Permissions\nR. Thompson   | Project Manager    | Full CRUD on all modules\nK. Patel      | Inspector          | View all, Upload docs, Create RFIs\nS. Chen       | Admin Assistant    | View all, Run reports, Export data\n\nProject Template: CIP-Federal-Grant\n- Standard CIP modules: Cost, Schedule, Documents, RFIs\n- Additional: Federal compliance checklist, DBE tracking, Davis-Bacon reporting\n- Approval workflow: Change Orders require 2 approvals (PM + Grants Manager)",
            "sql_or_config": "-- Setup in e-Builder Admin\n\n-- Step 1: Create users\n-- Admin > Users > Add New User\n-- R. Thompson: role = ProjectManager, projects = CIP-2024-004\n-- K. Patel: role = Inspector, projects = CIP-2024-004\n-- S. Chen: role = ViewerReporter, projects = CIP-2024-004\n\n-- Step 2: Clone project template\n-- Admin > Templates > Clone 'CIP-Standard'\n-- Name: 'CIP-Federal-Grant'\n-- Add modules: FederalCompliance, DBETracking, DavisBacon\n\n-- Step 3: Create the project\n-- Projects > New > Template: CIP-Federal-Grant\n-- Name: Padre Island Seawall Repair Phase II\n-- Budget: $6,200,000\n-- Fund Source: Federal Grant\n-- Start Date: 2026-03-15\n\n-- Step 4: Configure approval workflow\n-- Workflows > Change Order > Add step\n-- Approver 1: Project Manager (R. Thompson)\n-- Approver 2: Federal Grants Manager\n-- Threshold: All COs require dual approval",
            "why_correct": "Department onboarding follows a standard pattern: (1) identify users and roles using least-privilege principle, (2) configure the project from a template (never start from scratch), (3) customize for project-specific requirements (federal grant compliance in this case), and (4) document everything so the next onboarding is faster. The federal grant compliance addenda are critical — missing DBE tracking or Davis-Bacon reporting can result in loss of federal funding.",
            "what_to_document": "1. User Account Setup Guide (3 users with role matrix)\n2. Project Configuration Checklist\n3. Federal Grant Compliance Requirements Addendum\n4. Approval Workflow Documentation\n5. Training Guide: 'Getting Started with e-Builder for CIP Projects'\n6. Quick Reference Card for each user role",
            "what_to_test": "1. R. Thompson: log in, create a cost item, upload a document, submit a change order\n2. K. Patel: log in, view project, upload submittal, create RFI (should NOT be able to approve COs)\n3. S. Chen: log in, view project, run a report, export to CSV (should NOT be able to edit data)\n4. Verify federal compliance modules are visible and functional\n5. Test change order dual-approval workflow end-to-end",
            "mentor_explanation": "Department onboarding is a recurring task that seems routine but is actually high-impact. Get the roles wrong and someone can approve their own change orders (audit finding). Miss the compliance modules and the city could lose federal funding. The key skill is systematic setup using checklists and templates, followed by role-based testing to verify permissions. Always test WHAT USERS CANNOT DO as well as what they can do — negative testing catches permission gaps.",
            "interview_answer": "STAR: Situation — Engineering Department received $6.2M in federal grant funding and needed e-Builder access to manage the Seawall Repair project. Task — Onboard 3 users with appropriate roles, configure the project with federal compliance requirements, and document the process. Action — Created a role matrix with least-privilege access for each user type. Cloned the CIP template and added federal grant modules (DBE tracking, Davis-Bacon, compliance checklists). Configured dual-approval workflows for change orders. Tested each role to verify both positive and negative permissions. Created training documentation and quick reference cards. Result — Department onboarded in 2 days (down from the previous average of 5 days). The onboarding template and documentation reduced future onboarding time by 60%.",
            "skills_practiced": "requirements,process_docs,sharepoint,change_mgmt",
            "phase": "configuration",
        }],
    },
}
